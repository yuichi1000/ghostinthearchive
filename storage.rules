rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 画像・音声: 読み取りのみ許可（公開コンテンツ）
    // 書き込みは Admin SDK（パイプライン）のみ
    match /{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
